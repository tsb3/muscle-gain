<h1 align="center"><%= @category.name %></h1>
<div align="center">
<% if logged_in? and current_user.admin? %>
  <div class="row">
    <span class="badge"><%= link_to "カテゴリ名を編集する", edit_category_path(@category) %></span>
  </div>
<% end %>
  <%= paginate @category_articles %>
</div>
<%= render 'articles/article', obj: @category_articles %>
<div align="center">
  <%= paginate @category_articles %>
</div>